import React from 'react';
import TodoApp from './components/TodoApp/TodoApp';
import './App.css'

function App() {

  return (
    <TodoApp />
  // The TodoApp component is imported and rendered here.
  // This is the main entry point of the application.
  // It serves as the root component that contains the entire todo application.
  // The App component is styled using the App.css file.
  )
}

export default App
